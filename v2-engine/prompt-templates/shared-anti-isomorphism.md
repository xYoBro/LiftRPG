# 7. ANTI-ISOMORPHISM CHECK (MANDATORY)

Before finalizing your JSON, compute your STRUCTURAL SIGNATURE.

**Primitives Signature:**
List your Dice Type, Tracker Profile (fill vs drain vs heat vs tug), Resource Pattern, and Spatial Element.
Your signature MUST NOT just be the lazy default: "1d6 + 3 fill-clocks".
Change outcome counts, mix tracker types (1 drain + 1 tug), or use a Dual Economy to guarantee a mechanically unique game.

**Wire Topology (if wiring blueprint provided):**
List your wire implementation: How many threshold-gates? Feedback-loops? Conditional routes? Escalation wires? Unlock-chains?
Confirm each wire from the blueprint is implemented: name the mechanic that serves as `from`, the target that serves as `to`, and where the `printInstruction` appears in your output.
If a wire was specified in the blueprint but you did not implement it, explain why.

GENERATE THE JSON NOW:
